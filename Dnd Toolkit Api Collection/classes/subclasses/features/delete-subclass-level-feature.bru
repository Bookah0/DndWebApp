meta {
  name: delete subclass level feature
  type: http
  seq: 6
}

delete {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/levels/{{levelId}}/features/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
