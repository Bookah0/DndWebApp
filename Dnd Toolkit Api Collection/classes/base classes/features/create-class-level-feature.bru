meta {
  name: create class level feature
  type: http
  seq: 4
}

post {
  url: {{ENV}}/api/classes/{{classId}}/levels/{{levelId}}/features
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
