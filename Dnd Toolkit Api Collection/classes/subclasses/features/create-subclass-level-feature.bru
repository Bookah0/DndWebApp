meta {
  name: create subclass level feature
  type: http
  seq: 3
}

post {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/features
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
