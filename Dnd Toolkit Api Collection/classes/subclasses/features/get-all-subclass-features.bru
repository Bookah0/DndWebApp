meta {
  name: get all subclass features
  type: http
  seq: 1
}

get {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/features
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
