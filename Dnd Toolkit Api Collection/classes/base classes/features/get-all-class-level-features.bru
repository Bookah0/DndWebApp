meta {
  name: get all class level features
  type: http
  seq: 2
}

get {
  url: {{ENV}}/api/classes/{{classId}}/levels/{{levelId}}/features
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
