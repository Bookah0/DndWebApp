meta {
  name: update class level feature
  type: http
  seq: 5
}

put {
  url: {{ENV}}/api/classes/{{classId}}/levels/{{levelId}}/features/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
