meta {
  name: get all background features
  type: http
  seq: 1
}

get {
  url: {{ENV}}/api/backgrounds/{{backgroundId}}/features
  body: json
  auth: inherit
}

vars:pre-request {
  backgroundId: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
