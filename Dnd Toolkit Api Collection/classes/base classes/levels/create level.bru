meta {
  name: create level
  type: http
  seq: 3
}

post {
  url: {{ENV}}/api/classes/{{classId}}/levels
  body: json
  auth: inherit
}

body:json {
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
