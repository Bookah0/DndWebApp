meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{ENV}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "Email": "test@test.com",
    "Password": "123456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
