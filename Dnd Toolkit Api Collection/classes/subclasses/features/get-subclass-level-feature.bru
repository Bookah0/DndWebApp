meta {
  name: get subclass level feature
  type: http
  seq: 2
}

get {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/features/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
