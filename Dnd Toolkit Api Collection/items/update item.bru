meta {
  name: update item
  type: http
  seq: 4
}

patch {
  url: http://localhost:5432/items/{{itemId}}
  body: json
  auth: inherit
}

body:json {
  {
    "EquipSlot": "Armor"
  }
}

vars:pre-request {
  itemId: 38
}

settings {
  encodeUrl: true
  timeout: 0
}
