meta {
  name: get class level feature
  type: http
  seq: 3
}

get {
  url: {{ENV}}/api/classes/{{classId}}/levels/{{levelId}}/features/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
