meta {
  name: update level
  type: http
  seq: 4
}

patch {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/levels/{{levelId}}
  body: json
  auth: inherit
}

vars:pre-request {
  spellId: 51
}

settings {
  encodeUrl: true
  timeout: 0
}
