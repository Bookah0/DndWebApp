meta {
  name: update
  type: http
  seq: 4
}

patch {
  url: {{ENV}}/api/temps/{{tempId}}
  body: json
  auth: inherit
}

vars:pre-request {
  spellId: 51
}

settings {
  encodeUrl: true
  timeout: 0
}
