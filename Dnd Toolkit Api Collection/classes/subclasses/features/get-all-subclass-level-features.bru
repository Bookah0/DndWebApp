meta {
  name: get all subclass level features
  type: http
  seq: 2
}

get {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/levels/{{levelId}}/features
  body: none
  auth: inherit
}

vars:pre-request {
  subclassId: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
