meta {
  name: get level
  type: http
  seq: 1
}

get {
  url: {{ENV}}/api/classes/{{classId}}/subclasses/{{subclassId}}/levels/{{levelId}}
  body: none
  auth: inherit
}

vars:pre-request {
  spellId: 51
}

settings {
  encodeUrl: true
  timeout: 0
}
