meta {
  name: update subrace trait
  type: http
  seq: 4
}

put {
  url: {{ENV}}/api/races/{{raceId}}/subraces/{{subraceId}}/traits/{{traitId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
