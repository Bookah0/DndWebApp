meta {
  name: delete class level feature
  type: http
  seq: 5
}

delete {
  url: {{ENV}}/api/classes/{{classId}}/features/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
