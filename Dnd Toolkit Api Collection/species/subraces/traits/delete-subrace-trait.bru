meta {
  name: delete subrace trait
  type: http
  seq: 5
}

delete {
  url: {{ENV}}/api/races/{{raceId}}/subraces/{{subraceId}}/traits/{{traitId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
